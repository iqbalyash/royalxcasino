{
  "name": "royal-x-casino",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:cloudflare": "next build",
    "build:pages": "npm run build",
    "start": "next start",
    "lint": "next lint",
    "prebuild": "echo 'Building for Cloudflare Pages (not Workers)'",
    "deploy": "npm run build && echo 'Build complete. Use Cloudflare Pages dashboard to deploy from out/ directory.'"
  },
  "dependencies": {
    "next": "^16.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.0.0"
  }
}
